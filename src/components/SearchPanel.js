import React from 'react';

const SearchPanel = () => {
    const searchText = 'input here text for find';
    return (
      <input placeholder={searchText} />
    );
  };

  export default SearchPanel;